# 内容维护指南

这个应用现在会自动扫描 `content` 目录下的所有 `.md` 文件：

```text
content/**/*.md
```

也就是说，你想修改显示内容或添加自己的 Markdown 文档，主要改 `content` 目录即可

## 1. 修改已有显示内容

例如要修改 JavaScript 基础语法页面，打开：

```text
content/javascript/basics.md
```

文件顶部 `---` 到 `---` 之间是页面元信息，下面才是正文

```markdown
---
languageId: javascript
languageName: JavaScript
group: Web
accent: #f5c542
description: 浏览器与 Node.js 生态的主力脚本语言
topicId: basics
topicTitle: 基础语法
summary: 变量、类型、函数、控制流与模块
order: 10
---

# JavaScript 基础语法

这里写正文内容
```

你可以直接修改正文，保存后重新运行或重新打包即可

## 2. 给已有语言添加一个新主题

例如给 Python 添加“文件操作”主题：

1. 新建文件：

```text
content/python/file-io.md
```

2. 写入内容：

````markdown
---
languageId: python
languageName: Python
group: General
accent: #4b8bbe
description: 强调可读性、脚本效率和生态广度的通用语言
topicId: file-io
topicTitle: 文件操作
summary: open、with、读写文本与异常处理
order: 30
---

# Python 文件操作

## 读取文本

```python
with open("demo.txt", "r", encoding="utf-8") as file:
    content = file.read()
```

## 写入文本

```python
with open("demo.txt", "w", encoding="utf-8") as file:
    file.write("hello")
```
````

保存后，这个主题会自动出现在 Python 的主题列表里

## 3. 添加一门新语言

例如添加 TypeScript：

1. 新建目录：

```text
content/typescript
```

2. 新建文件：

```text
content/typescript/basics.md
```

3. 写入内容：

````markdown
---
languageId: typescript
languageName: TypeScript
group: Web
accent: #3178c6
description: 为 JavaScript 添加静态类型系统的语言
topicId: basics
topicTitle: 基础语法
summary: 类型标注、接口、泛型与联合类型
order: 10
---

# TypeScript 基础语法

## 类型标注

```typescript
const name: string = "Ada";
const age: number = 36;
```

## 接口

```typescript
interface User {
  id: number;
  name: string;
}
```
````

保存后，左侧语言列表会出现 TypeScript

## 4. 元信息字段说明

| 字段 | 作用 |
| --- | --- |
| `languageId` | 语言唯一标识，建议小写英文，例如 `python`、`typescript` |
| `languageName` | 左侧显示的语言名称 |
| `group` | 语言分组，例如 `Web`、`Backend`、`Systems` |
| `accent` | 左侧颜色条，使用十六进制颜色 |
| `description` | 中间栏语言简介 |
| `topicId` | 主题唯一标识 |
| `topicTitle` | 中间栏显示的主题名称 |
| `summary` | 主题摘要 |
| `order` | 排序数字，越小越靠前 |

## 5. 修改后如何查看

开发预览：

```cmd
cd **\syntax-md-desktop
npm.cmd run dev
```

浏览器打开：

```text
http://localhost:1420
```

如果要生成新的 exe：

```cmd
cd **\syntax-md-desktop
npm.cmd run tauri build
```

生成后的 exe 通常在：

```text
src-tauri\target\release\syntax-md-desktop.exe
```

安装包通常在：

```text
src-tauri\target\release\bundle\nsis\
```

## 6. 注意事项

- 每篇 Markdown 顶部建议保留 `---` 元信息
- 同一门语言的 `languageId` 必须一致
- 同一门语言下不同主题的 `topicId` 不要重复
- 正文可以正常使用标题、表格、列表和代码块
- 修改内容后，已经打包好的旧 exe 不会自动变化，需要重新执行 `npm.cmd run tauri build`
